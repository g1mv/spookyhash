language: c

# compiler:
#   - clang
#   - gcc
  
# before_install:
#   - sudo apt-get -qq --yes install premake5
  
# before_script:
#   - cd build && premake5 gmake

script:
  - cd src
  - gcc -std=c99 *.c ../test/src/*.c ../test/src/cputime/src/*.c
  - clang *.c ../test/src/*.c ../test/src/cputime/src/*.c
  - rm a.out
